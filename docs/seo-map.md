# SEO & Schema Map — Capital Operator

## Meta Configuration
Every page renders dynamic `<title>`, `<meta name="description">`, OpenGraph tags (`og:title`, `og:image`, `og:url`), and Twitter Cards via the `<SEOHead />` component using centralized configurations from `src/config/seo.ts`.

## Structured Data (Schema.org)
- **Organization**: Moonshine Capital (Financial Services, Commercial Lending)
- **SoftwareApplication**: Capital Operator (Fintech Infrastructure Assessment)
- **BreadcrumbList**: Hierarchical navigational trail across all sub-pages.
